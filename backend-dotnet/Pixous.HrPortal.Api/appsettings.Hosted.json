{
  "//": "The HOSTED production database (db_ab2fe4_ems). NOTHING SECRET HERE -- this file is committed. The connection string arrives at runtime from .env via run-hosted.bat, which resolves the host to IPv4 first.",

  "//Logging": "Information for our own code so a hosted run is traceable; Warning for the framework.",

  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning",
      "Pixous.HrPortal": "Information"
    }
  },

  "AllowedHosts": "*",

  "//Database.ReadOnly": "FALSE here because the hosted account can write and the application must behave exactly as it will in production. Set it true only when pointing at a reporting replica.",

  "Database": {
    "ReadOnly": false
  }
}
